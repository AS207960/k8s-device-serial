apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
spec:
  containers:
    - name: demo-container-1
      image: busybox
      tty: true
      stdin: true
      resources:
        limits:
          as207960.net/serial: 1
